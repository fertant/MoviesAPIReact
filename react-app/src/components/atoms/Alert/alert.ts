import styled from 'styled-components';

export const AlertPopup = styled.div`
  position: absolute;
  top: 0;
  padding: 15px;
  text-align: center;
  width: 100%;
  color: white;
  z-index: 5;
`;

export default AlertPopup;
